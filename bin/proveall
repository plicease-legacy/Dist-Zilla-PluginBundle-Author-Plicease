#!/usr/bin/perl

use strict;
use warnings;
use App::Plicease::Dev::ProveAll;

# PODNAME: proveall
# ABSTRACT: run prove on all tests in the dev directory.

App::Plicease::Dev::ProveAll->main(@ARGV);

__END__

=head1 SYNOPSIS

 % proveall
 % proveall --help

=head1 DESCRIPTION

Find all the tests for all distributions in the ~/dev directory
and run them using prove -b.  If Build.PL or Makefile.PL is
included with any of the distribtions, they will be built
appropriately first.

=head1 OPTIONS

=head2 --help

Display this help message.

=cut


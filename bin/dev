#!/usr/bin/perl

use strict;
use warnings;
use App::Plicease::Dev;

# PODNAME: dev
# ABSTRACT: Update PATH and PERL5LIB for development distributions.
# VERSION

App::Plicease::Dev->main(@ARGV);

__END__

=head1 SYNOPSIS

 % dev [ --sh | --csh | --cmd ] [ /path/to/dev/root ]
 % dev --help

 # from .profile or .cshrc
 eval `dev`

 # from windows
 dev > devconfig.bat
 devconfig.bat

=head1 DESCRIPTION

This script scans through the Perl distributions in the given directory
and scans them for lib and bin directories adding them to the PATH and
PERL5LIB directories as appropriate so that development and testing can
be done with them without running Makefile.PL/make or using L<Blib>.

=head1 OPTIONS

=head2 --sh

Use sh compatible format used by bash, ash, sh and others.

=head2 --csh

Use csh compatible format used by tcsh and csh.

=head2 --cmd

Use cmd compatible format used by Windows.

=head2 --help

Display this help message.

=head1 SEE ALSO

L<App::Plicease::Dev>

=cut

